services:
  hthompson:
#    image: strangeranger/hthompson:latest
    image: hthompson:25-alpine
    container_name: hthompson
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL