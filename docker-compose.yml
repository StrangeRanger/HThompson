services:
  hthompson:
    image: strangeranger/hthompson:latest
    container_name: hthompson
    restart: unless-stopped
    ports:
      - "3000:3000"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

